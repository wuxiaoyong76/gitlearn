一、本地新建仓库

git init 当前目录作为仓库，初始化
git init <目录> 指定目录作为Git仓库

建仓库成功后，会出现一个.git文件目录，就是上次说的版本库


要想把文件纳入版本控制需提交：

git add 文件名/*.扩展名/文件夹

提交到暂存区

git commit -m "提交说明"


二、从现在的库中copy项目

git clone <repo>
git clone <repo> <directory> 项目重命名为<directory>